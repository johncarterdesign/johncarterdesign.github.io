var sliderWrap = document.getElementById('sliderWrap'); var sliderKnobWrap = document.getElementById('sliderKnobWrap'); var sliderKnob = document.getElementById('sliderKnob'); var sliderHash = document.getElementById('sliderHash'); var sliderHashLinks = sliderHash.getElementsByTagName('li'); var sliders = sliderWrap.getElementsByClassName('slider'); var sliderKnobWrapLeft; var sliderKnobWrapWidth; var activeSliders; var sliderSplit; var knobActive; function setSliderGlobals() { sliderKnobWrapLeft = sliderKnobWrap.getBoundingClientRect().left; sliderKnobWrapWidth = sliderKnobWrap.getBoundingClientRect().width; activeSliders = document.getElementsByClassName('slider active'); if (activeSliders.length == 1) { sliderKnobWrap.classList.add('inactive'); knobActive = false; } else { sliderKnobWrap.classList.remove('inactive'); knobActive = true; } sliderSplit = sliderKnobWrapWidth/activeSliders.length; } setSliderGlobals(); window.addEventListener('resize', setSliderGlobals); function moveKnob(e) { var knobPos = 0; if (e.clientX <= sliderKnobWrapLeft + 12) { sliderKnob.style.right = 'auto'; sliderKnob.style.left = '0px'; knobPos=0; } else if (e.clientX>= sliderKnobWrapLeft + sliderKnobWrapWidth - 12) { sliderKnob.style.left = 'auto'; sliderKnob.style.right = '0px'; knobPos = sliderKnobWrapWidth; } else { sliderKnob.style.right = 'auto'; sliderKnob.style.left = (e.clientX - sliderKnobWrapLeft - 9) + 'px'; knobPos = e.clientX - sliderKnobWrapLeft; } for (var i = 0; i < activeSliders.length; i++) { if (knobPos>= sliderSplit * i && knobPos < sliderSplit * (i+1)) { sliderWrap.className = 'full pos-' + i; } } } function sliderMouseDown(e) { if (knobActive) { moveKnob(e); sliderKnobWrap.addEventListener('mousemove', moveKnob); window.addEventListener('mouseup', function(e) { sliderKnobWrap.removeEventListener('mousemove', moveKnob) }); } } sliderKnobWrap.addEventListener('mousedown', sliderMouseDown); for (var i = 0; i < sliderHashLinks.length; i++) { sliderHashLinks[i].addEventListener('click', function() { var oldTarget = document.getElementsByClassName('sliderHash active')[0]; var target = this.getAttribute('data-target'); for (var i = 0; i < sliders.length; i++) { if (sliders[i].getAttribute('data-type') == target) { sliders[i].classList.add('active') } else { sliders[i].classList.remove('active') } } oldTarget.classList.remove('active'); this.classList.add('active'); sliderWrap.className = 'full pos-0'; sliderKnob.style.right = 'auto'; sliderKnob.style.left = '0px'; setSliderGlobals() }); }